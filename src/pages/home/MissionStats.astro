---
import Button from '../../components/global/UI/Button.astro';
---
<!-- Mission & Stats Section -->
<section class="mission-section">
  <div class="mission-container">
    <div class="mission-grid">
      
      <!-- Text Content -->
      <div class="mission-text-box">
        <span class="mission-overline">OUR MISSION</span>
        <h2 class="mission-title">Powering Progress,<br/>Powering You</h2>
        <p class="mission-desc">
          Our advanced energy storage solutions capture and store surplus electricity generated by solar panels, providing a reliable source of power during low-light periods and outages.
        </p>
        <p class="mission-desc">
          By enhancing self-sufficiency and reducing dependence on conventional grids, Mahati Enterprises is a driving force in the green energy revolution, committed to a future of dependable and resilient clean energy.
        </p>
        <div class="mission-btn-wrap">
          <Button href="/about" showArrow={true}>
            More About Us
          </Button>
        </div>
      </div>

      <!-- Stats Grid (3 White Elevated Cards) -->
      <div class="stats-grid">
        
        <div class="stat-card">
          <span class="material-symbols-outlined stat-icon">groups</span>
          <div class="stat-number">100+</div>
          <div class="stat-title">Satisfied Customers</div>
        </div>

        <div class="stat-card">
          <span class="material-symbols-outlined stat-icon">solar_power</span>
          <div class="stat-number">15+</div>
          <div class="stat-title">Running Projects</div>
        </div>

        <div class="stat-card">
          <span class="material-symbols-outlined stat-icon">military_tech</span>
          <div class="stat-number">10+</div>
          <div class="stat-title">Years of Experience</div>
        </div>

      </div>

    </div>
  </div>
</section>

<style>
  /* Base Section Background matching the light tone in the image */
  .mission-section {
    background-color: var(--color-surface-container-low, #f4f7fc);
    padding: 80px 24px;
    width: 100%;
  }

  .mission-container {
    max-width: 1280px;
    margin: 0 auto;
  }

  /* Grid Layout */
  .mission-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 48px;
    align-items: center;
  }

  @media (min-width: 992px) {
    .mission-grid {
      grid-template-columns: 1fr 1fr;
      gap: 64px;
    }
  }

  /* Left Text Styling */
  .mission-text-box {
    max-width: 540px;
  }

  .mission-overline {
    font-family: var(--font-body, 'Hanken Grotesk', sans-serif);
    font-size: 14px;
    font-weight: 700;
    color: var(--color-secondary-container, #fd761a);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    display: block;
    margin-bottom: 12px;
  }

  .mission-title {
    font-family: var(--font-heading, 'Manrope', sans-serif);
    font-size: 36px;
    line-height: 44px;
    font-weight: 700;
    color: var(--color-primary, #0b1727);
    margin-bottom: 24px;
    letter-spacing: -0.01em;
  }

  .mission-desc {
    font-family: var(--font-body, 'Hanken Grotesk', sans-serif);
    font-size: 16px;
    line-height: 26px;
    color: var(--color-on-surface-variant, #4a5568);
    margin-bottom: 16px;
  }

  .mission-btn-wrap {
    margin-top: 32px;
  }



  /* 3 Stats Cards Grid */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }

  .stat-card:last-child:nth-child(odd) {
    grid-column: 1 / -1;
  }

  /* Solid White Elevated Cards matching the design image */
  .stat-card {
    background: #ffffff;
    border-radius: 20px;
    padding: 36px 20px;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.04), 0 2px 6px rgba(0, 0, 0, 0.02);
    border: 1px solid rgba(0, 0, 0, 0.03);
    transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.35s ease, border-color 0.35s ease;
    cursor: default;
  }

  /* Hover Animations & Lift Effect */
  .stat-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(11, 23, 39, 0.08), 0 6px 16px rgba(11, 23, 39, 0.04);
    border-color: rgba(253, 118, 26, 0.2);
  }

  /* Icon Styling with subtle hover animation */
  .stat-icon {
    font-size: 38px !important;
    color: var(--color-secondary-container, #fd761a);
    margin-bottom: 12px;
    transition: transform 0.3s ease;
  }

  .stat-card:hover .stat-icon {
    transform: scale(1.15);
  }

  /* Number Value */
  .stat-number {
    font-family: var(--font-heading, 'Manrope', sans-serif);
    font-size: 26px;
    font-weight: 700;
    line-height: 32px;
    color: var(--color-primary, #0b1727);
    margin-bottom: 6px;
  }

  /* Label Text */
  .stat-title {
    font-family: var(--font-body, 'Hanken Grotesk', sans-serif);
    font-size: 11px;
    font-weight: 600;
    color: rgba(74, 85, 104, 0.75);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
</style>